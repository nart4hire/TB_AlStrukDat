#include <stdio.h>
#include "wordmachine.h"

int main(int argc, char const *argv[])
{
    startFile();
    while (!endLine)
    {
        printf("I got here");
        advFile();
    }
    
    return 0;
}
