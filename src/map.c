#include <stdio.h>
#include "point.h"
#include "matrix.h"

Matrix parseMap()
{
    // returns map from config coords
}

Matrix parsePath()
{
    // returns adjacency matrix
}

void Displaymap(POINT currentLocation, Matrix Map, Matrix Adjacency)
{
    
}